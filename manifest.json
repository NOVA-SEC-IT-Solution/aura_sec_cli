{
  "version": "1.0",
  "roles": [
    {
      "name": "Backend",
      "display_name": "Backend Service",
      "markers": [
        "Cargo.toml",
        "go.mod",
        "pom.xml"
      ],
      "quality_command": "cargo check --message-format=json",
      "checklist_path": "templates/backend.md"
    },
    {
      "name": "Frontend",
      "display_name": "Frontend Application",
      "markers": [
        "package.json",
        "tsconfig.json"
      ],
      "quality_command": "npx tsc --noEmit",
      "checklist_path": "templates/frontend.md"
    },
    {
      "name": "Infrastructure",
      "display_name": "Infrastructure (Terraform)",
      "markers": [
        "terraform.tf",
        "main.tf",
        "Dockerfile"
      ],
      "quality_command": "terraform validate -json",
      "checklist_path": "templates/infra.md"
    }
  ],
  "secret_patterns": [
    {
      "name": "AWS Access Key",
      "regex": "(?i)AKIA[0-9A-Z]{16}",
      "severity": "Critical"
    },
    {
      "name": "RSA Private Key",
      "regex": "-----BEGIN (RSA )?PRIVATE KEY-----",
      "severity": "Critical"
    },
    {
      "name": "Generic Password/Token",
      "regex": "(?i)(password|secret|token|api_key|apikey)[\\s:=]*[\"'][\\w\\-]{16,}[\"']",
      "severity": "High"
    },
    {
      "name": "GitHub Token",
      "regex": "ghp_[a-zA-Z0-9]{36}",
      "severity": "Critical"
    },
    {
      "name": "Slack Token",
      "regex": "xox[baprs]-[0-9]+-[0-9]+-[a-zA-Z0-9]+",
      "severity": "Critical"
    },
    {
      "name": "Stripe Secret Key",
      "regex": "sk_live_[0-9a-zA-Z]{24}",
      "severity": "Critical"
    },
    {
      "name": "Database Connection String",
      "regex": "(?i)(postgres|mysql)://[^\\s]+:[^\\s]+@[^\\s]+",
      "severity": "Critical"
    }
  ],
  "ai_prompt": "you are a world-class security engineer and penetration tester. Your mission is to conduct a deep security analysis of the provided [ROLE] code. \n\nFocus on: Injections, Broken Access Control, Insecure Crypto, and Leaked Logic. \n\nFormat Requirements: \n- Return ONLY a raw JSON array of findings. \n- Each finding must have: \"file_path\", \"line_number\", \"message\", \"severity\" (High, Medium, or Low). \n- If no issues found, return []. \nNo conversational text, no markdown code block markers.",
  "checklist_prompt": "Based on these git changes:\n```\n[DIFF]\n```\n\nAnd this existing security checklist:\n```\n[CHECKLIST]\n```\n\nSuggest highly-actionable, short, contextual security verification steps that a developer must strictly verify before merging. Format your output strictly as a Markdown list, one per line, with no introductory or wrapping text. Each item MUST start with '- '. Example: '- Ensure the rate limiter tracks IP addresses using X-Forwarded-For.'"
}
